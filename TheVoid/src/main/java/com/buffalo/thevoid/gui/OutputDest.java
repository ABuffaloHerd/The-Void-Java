package com.buffalo.thevoid.gui;

/**
 * Choose destination for output.
 * Can be wrapped into a record.
 * Used by program and gui to choose where to send data.
 */
public enum OutputDest
{
    CONSOLE,
    FILE,
    GUI_LOG
}
